1、node  js的解释器   chrome v8
var console={
	log:function(){

	}
}


console.log(data,callback(data1))

2、解决高并发（异步处理 事件循环）
  node做中间层（新老需求拟合）
  node前后端分离（无耦合）

  后端渲染  jsp  php  asp  
  前端渲染  

  <div><?php echo $data?></div>

  [
  	{
  		coffername:'blue',
  		coffercooker:'wuyanzu',
  	},
  	{

  	}
  ]

3、数据包（包头、包体）\ node是单进程+webpack热更新--- pm2 nodemon  supervisor
npm install -g supervisor 

4、Function 函数 类 构造函数

5、原生模块、第三方模块（node_modules）、自定义模块

  npm install -g forever
  npm install forever
  npm install forever --save     package.json

  6、node下有哪些模块是不需要加载（自动加载）
    global  process  timer  console module buffer

  7、三码一致

    文件编码 ANSI Unicode UTF-8
    浏览器解码 gbk/gb2312/gb18030  UTF-8
    数据库解码 UTF-8
  8、是不是callback一定是异步的？No

  9、http四大关键字（get post put delete）
    ip(ipv4  ipv6)
  10、 get 数据只能在url上出现 4096byte  1byte=8bit  1int=4byte  2byte  8byte
      post 数据安全性 传输数据量大 
  11、前端跨域 iframe script、postMessage、$.getUrl、
     后端跨域  Allow-Origin、cors